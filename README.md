# Rosetta

Basic instructions.

## Setup

`npm install`

## Local Server

To run a local server on port 5000 using BrowserSync:

`npm run dev`

## Compile TypeScript

To compile the typescript:

`npm run build`